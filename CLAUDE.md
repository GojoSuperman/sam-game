# sam-game — 프로젝트 규칙

SPUM Studio 를 코드로 몰아 맵을 만드는 프로젝트.
세부 실측은 [docs/Studio와 Claude Code 역할 분담.md](docs/Studio와%20Claude%20Code%20역할%20분담.md) 에 있다.

## ★ 맵 제작 요청을 받으면 — 먼저 묻는다

"이런 느낌으로 맵 만들어줘" 류의 요청에 **바로 프롬프트를 쓰지 않는다.**
씬 맵은 회당 약 250쌤이 들고 되돌리기가 번거롭다. 그림이 나온 뒤에
"이게 아니었는데" 가 되면 그 비용이 그대로 날아간다.

`AskUserQuestion` 으로 **최대 4가지**를 먼저 확인한다:

| 물을 것 | 왜 |
|---|---|
| **구역 구성** | 프롬프트의 뼈대다. "무엇이 어디에 있어야 하나" 가 결과를 가장 크게 좌우한다 (바 카운터는 우상, 계단은 우하 …) |
| **양식·시대** | 중세/현대/SF/판타지 + 구체 스타일. 같은 "주점" 도 완전히 달라진다 |
| **분위기·조명** | 낮/밤, 따뜻함/차가움. 밝은 씬일수록 **마스크 정확도가 높다** (실측) |
| **방식** | 씬(고품질·약 250쌤·그 맵 전용) vs 타일(무료·재사용·품질 낮음) |

이미 사용자가 답을 준 항목은 다시 묻지 않는다. 모호한 것만 묻는다.
답을 받으면 프롬프트 초안을 **보여주고** 실행한다 (과금 직전이므로).

## 맵 만들기 — 한 방 명령

```bash
npm run scene-map -- --name "<맵 이름>" --prompt-file <파일> --headed --record
```

창 하나가 뜨고 ① 씬 생성 → ② 참조본 축소 → ③ 마스크 생성 → ④ 통행 판정·고립 잇기
→ ⑤ 주입 → ⑥ 확인 스크린샷 까지 이어진다. 사용자가 전 과정을 지켜본다.
`--dry-run` 은 그림만 만들고 주입하지 않는다.

## 지켜야 할 것

- **프롬프트는 520자 이하.** 507자에서 `504`, 446자에서 성공 (실측). 구역 배치를 남기고 수식어를 줄인다.
- **참조 이미지는 512² 로.** 1024²(1.7MB) 를 그대로 올리면 `413`.
- **`Classify` 는 누르지 않는다.** 분류기가 러그를 장애물로, 돌바닥을 blocked 로 붙인 사례가 있다.
- **프리셋(`#resourcePresetSelect`)은 건드리지 않는다.** 고르면 프롬프트가 덮인다.
- **`npm run studio-open` 창을 열어둔 채 작업하지 않는다.** 같은 프로필을 두 크롬이 잡으면
  저장이 어디에도 안 남는다 (한나절을 잡아먹은 원인). 이제 스크립트가 시작 전에 막는다.
- **생성한 그림은 즉시 파일로.** 비용이 든 산출물이고 Studio 로컬은 날아갈 수 있다.
- localStorage 한도는 **약 5MB**. 씬 맵 하나가 500KB 정도다.

## 언어

문서·주석·로그·UI 텍스트는 한국어. 코드 식별자는 영어.
